# Git Cheatsheet

## Clonar
git clone URL

## Fluxo básico
git status
git add .
git commit -m "msg"
git push
git pull

## Branch
git branch
git checkout -b nova
git merge nova

## Remotes
git remote -v
git remote set-url origin URL

## Docs oficiais
https://git-scm.com/docs
